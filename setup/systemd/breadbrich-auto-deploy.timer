[Unit]
Description=Run breadbrich-auto-deploy every 2 minutes
Requires=breadbrich-auto-deploy.service

[Timer]
# First tick 1 minute after boot, then every 2 minutes.
OnBootSec=1min
OnUnitActiveSec=2min
AccuracySec=10s
Unit=breadbrich-auto-deploy.service

[Install]
WantedBy=timers.target
